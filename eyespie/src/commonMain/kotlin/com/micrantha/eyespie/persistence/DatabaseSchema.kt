package com.micrantha.eyespie.persistence

/**
 * Current SQLDelight schema identity used by release evidence and local diagnostics.
 * Keep this synchronized with the checked-in migration set; candidate verification
 * remains the release authority for detecting drift.
 */
const val EYESPIE_DATABASE_SCHEMA_VERSION = 4
